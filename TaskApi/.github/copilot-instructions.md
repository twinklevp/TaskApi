# Copilot Instructions

## General Rules

1. Always use clean architecture principles (separate layers: API, Services, Data).
2. Prefer async/await for all I/O operations.
3. Use meaningful variable and method names (no abbreviations).
4. Add comments only when logic is non-obvious.
5. Follow .NET naming conventions (PascalCase for methods, camelCase for variables).