@page
@model TaskApi.Pages.Tasks.CreateModel
@{
    ViewData["Title"] = "New Task";
}

<div class="page-header">
    <div>
        <h1 class="page-title">New Task</h1>
        <p class="page-subtitle">Fill in the details below</p>
    </div>
    <a href="/Tasks" class="btn btn-ghost">← Back</a>
</div>

<div class="card form-card">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

        <div class="form-group">
            <label asp-for="Input.Title" class="form-label">Title <span class="required">*</span></label>
            <input asp-for="Input.Title" class="form-control" placeholder="e.g. Fix login bug" autofocus />
            <span asp-validation-for="Input.Title" class="form-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="Input.Description" class="form-label">Description</label>
            <textarea asp-for="Input.Description" class="form-control" rows="3" placeholder="Optional details…"></textarea>
        </div>

        <div class="form-row">
            <div class="form-group">
                <label asp-for="Input.Priority" class="form-label">Priority</label>
                <select asp-for="Input.Priority" class="form-control"
                        asp-items="Html.GetEnumSelectList<TaskPriority>()"></select>
            </div>
            <div class="form-group">
                <label asp-for="Input.Status" class="form-label">Status</label>
                <select asp-for="Input.Status" class="form-control"
                        asp-items="Html.GetEnumSelectList<Models.TaskStatus>()"></select>
            </div>
            <div class="form-group">
                <label asp-for="Input.DueDate" class="form-label">Due Date</label>
                <input asp-for="Input.DueDate" type="date" class="form-control" />
            </div>
        </div>

        <div class="form-group">
            <label asp-for="Input.Tags" class="form-label">Tags</label>
            <input asp-for="Input.Tags" class="form-control" placeholder="Comma-separated: bug, api, devops" />
            <span class="form-hint">Separate tags with commas</span>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Create Task</button>
            <a href="/Tasks" class="btn btn-ghost">Cancel</a>
        </div>
    </form>
</div>